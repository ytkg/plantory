# Plantory repository rules

## worktreeでの開発

- 実装・修正を伴うタスクは、共有のmainチェックアウトでは作業しない。着手前に最新のmainを起点とする専用ブランチと専用worktreeを作成し、そのworktreeだけを変更する。
- 1タスク（通常は1 Issue）につき、1ブランチ・1worktreeを使う。別タスクの変更や未コミット変更を混ぜない。
- 読み取り専用の調査、レビュー、ユーザーが共有チェックアウトでの作業を明示した場合、または既に対象の専用worktreeを指定された場合だけ、この作成手順を省略できる。
- worktreeを作成したら、対応するIssue、ブランチ名、作業ディレクトリを明確にしてから編集を始める。秘密情報はGit管理せず、必要になるまで別worktreeへ複製しない。
- 開発サーバー、実機、ローカルDBなどの共有資源を使う場合は、他のworktreeの作業と競合しないことを確認する。本番DB・本番Worker・実機への変更は、Issueの目的に必要な場合だけ行う。
- 作業完了後にworktreeやブランチを整理する前に、未コミット変更と対応PRの状態を確認する。未確認の変更を強制削除しない。

## 仕様の更新

- `docs/spec.md` をプロダクト仕様の正本として扱う。
- 画面、API、認証、データモデル、運用手順の振る舞いを追加・変更・削除したときは、同じ変更内で `docs/spec.md` も更新する。
- 実装前の案ではなく、現在リポジトリに実装されている仕様だけを記載する。
- 変更の完了前に、仕様書と実装が矛盾していないことを確認する。
